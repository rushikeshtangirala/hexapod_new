# ROS 2 / colcon build artefacts
build/
install/
log/
*.pyc
__pycache__/

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
~$*

# Superseded STL exports kept only for provenance.
# The URDF references the re-origined versions (coxa.stl, femur_lower.stl,
# femur_upper.stl, tibia.stl, body.stl). Files with spaces in the name break
# package:// resolution and must never be referenced.
